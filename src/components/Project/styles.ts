import { SxProps, Theme } from "@mui/system";
const styles: { [id: string]: SxProps<Theme> } = {
    root: {
        width: 400,
        borderRadius: 3,
    },
    image: {
        height: 200,
    },
};
export default styles;
